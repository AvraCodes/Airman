# Known Limitations

- Uses SQLite by default instead of PostgreSQL.
- Demo auth headers used for role identity; JWT scaffold only.
- Frontend is functional scaffold and not fully styled.
- No Alembic migrations yet.
