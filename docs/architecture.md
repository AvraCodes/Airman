# Architecture

Backend: FastAPI + SQLAlchemy service layer enforcing RBAC and sortie transitions.
Frontend: React + TypeScript pages for sortie board, aircraft readiness, training progress, and audit logs.
